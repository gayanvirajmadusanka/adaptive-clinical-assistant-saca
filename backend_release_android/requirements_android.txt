# Android APK requirements for backend_release
# Used by Chaquopy's python { pip { install "..." } } in build.gradle
#
# REPLACED from backend/requirements.txt:
#   openai-whisper  → Android native STT via React Native (faster-whisper blocked: tokenizers is Rust/maturin, no Android ARM64 wheel)
#   librosa         → python_speech_features + scipy  (pure Python/numpy, no C ext issues)
#   pydub           → stdlib wave + numpy  (no dependency needed)
#   webrtcvad_wheels stays (has Android wheels)
#
# DROPPED (dev/analysis only):
#   matplotlib, seaborn, pandas, pytest, gtts
#   imbalanced-learn: training-only, not used at inference time
#
# Native packages (numpy, scipy, sklearn, xgboost, soundfile, webrtcvad-wheels)
# are unpinned so Chaquopy resolves the latest available pre-built Android wheel.
# Pure-Python packages keep version pins for API stability.

fastapi==0.115.0
uvicorn==0.33.0
scikit-learn
xgboost
numpy
scipy
rapidfuzz
python_speech_features
pydantic==1.10.21
soundfile
